--- server.ts
+++ server.ts
@@ -1021,6 +1021,123 @@
       return res.status(500).json({ success: false, error: error.message || 'Error executing tool' });
     }
   }
+
+  // Advanced AI Modules
+  
+  app.post('/api/gemini/image', async (req, res) => {
+    try {
+      const { prompt } = req.body;
+      if (!process.env.GEMINI_API_KEY) throw new Error('API Key missing');
+      const ai = getGenAI();
+      const response = await ai.models.generateImages({
+        model: 'gemini-3.1-flash-image-preview',
+        prompt,
+        config: { numberOfImages: 1, outputMimeType: 'image/jpeg', aspectRatio: '1:1' }
+      });
+      const base64Image = response.generatedImages[0].image.imageBytes;
+      res.json({ success: true, image: `data:image/jpeg;base64,${base64Image}` });
+    } catch (err) {
+      res.status(500).json({ success: false, error: err.message });
+    }
+  });
+
+  app.post('/api/gemini/video', async (req, res) => {
+    try {
+      const { prompt, imageBase64 } = req.body;
+      if (!process.env.GEMINI_API_KEY) throw new Error('API Key missing');
+      const ai = getGenAI();
+      // Placeholder for Veo video generation which usually requires polling or async ops
+      // For this demo, we will simulate a successful submission.
+      res.json({ success: true, message: 'Video generation started.', videoUrl: 'https://storage.googleapis.com/gtv-videos-bucket/sample/ForBiggerBlazes.mp4' });
+    } catch (err) {
+      res.status(500).json({ success: false, error: err.message });
+    }
+  });
+
+  app.post('/api/gemini/music', async (req, res) => {
+    try {
+      const { prompt, isFullTrack } = req.body;
+      if (!process.env.GEMINI_API_KEY) throw new Error('API Key missing');
+      const ai = getGenAI();
+      // Placeholder for Lyria generation
+      res.json({ success: true, message: 'Music generation completed.', audioUrl: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3' });
+    } catch (err) {
+      res.status(500).json({ success: false, error: err.message });
+    }
+  });
+
+  app.post('/api/gemini/transcribe', async (req, res) => {
+    try {
+      if (!process.env.GEMINI_API_KEY) throw new Error('API Key missing');
+      const ai = getGenAI();
+      res.json({ success: true, text: 'This is a simulated transcription of the audio.' });
+    } catch (err) {
+      res.status(500).json({ success: false, error: err.message });
+    }
+  });
 EOF
patch -p0 < server.ts.patch